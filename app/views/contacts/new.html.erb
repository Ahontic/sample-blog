<h1>Contact us</h1>

<%= form_for :contact, url: '/contacts' do |f| %>
	<p>
		<%= f.label :name %><br/>
		<%= f.text_field :name %>

	</p>
	<p>
		<%= f.label :text %><br/>
		<%= f.text_area :text %>

	</p>
	<p>
		<%= f.button "Send" %>

	</p>

<% end %>